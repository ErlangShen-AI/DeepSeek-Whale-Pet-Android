# 更新日志

## 1.2.1 · 2026-09-16

### 声音

- 修复部分设备点击桌宠没有音效：按压音效原先按系统辅助提示音通道输出，可被系统策略静音且不归媒体音量管理，部分设备外放听不到但录屏能录到；现改用媒体通道，音量由媒体滑块统一控制，外放与录屏行为在所有设备上一致

## 1.2.0 · 2026-09-15

### 桌宠

- 悬浮桌宠常驻其他应用上层，拖动时与手指一比一跟随，松手按落点吸附到屏幕边缘，贴左缘整体镜像，气泡文字同步反向翻转后仍可读
- 按压形变以画布底边中心为原点，按下瞬间与音效同时开始，松手回弹
- 两个窗口的位置与尺寸每帧按画布几何结算，命中判定按实际绘制像素进行：鲸鱼读贴图透明度，气泡读三个图形的绘制掩码，透明像素不响应也不遮挡下层应用
- 镜像翻转与按压形变期间窗口取两侧并集，动画不被裁切；窗口被系统回收时自动重新挂上

### 气泡

- 气泡由代码绘制：大椭圆、尾巴与两个小圆沿用原挂件几何，三行文字随开合淡入，文字按椭圆弦长自适应，超宽缩字号、极端情况省略号收尾
- 余额默认每 60 秒刷新（可调 20 至 600 秒），单击鲸鱼立即刷新，余额变化时数字滚动
- 检测到余额下降时弹出消耗气泡，金额为红色正数
- 单击气泡第一次切到随机台词（含时段提示、动态图与卖萌台词），再点一次收起；消耗气泡点击直接收起
- 自定义余额、只显示台词与显示今日已用开关：自定义文本替代余额与今日已用，隐藏余额后气泡内不出现任何余额内容

### 设置与声音

- 设置界面按卡片分组：桌宠、权限、余额、用量、声音、后台运行、关于与使用说明
- 深浅色主题、系统字号缩放与旋转自适应，间距、圆角、字号全部由屏幕短边派生
- 两套音效可切换、可调音量、可试听；通知栏常驻条目提供刷新与收起操作
- 系统关闭动画时，位移、形变与数字滚动直接落位，气泡文字淡入不再等待延迟

### 工程

- 单元测试覆盖峰谷定价、余额差值记账与气泡内容取值规则，构建前先运行测试
- GitHub Actions 每天定时构建，校验签名主体与可调试标记后发布到 Releases
- 可恢复失败统一记录到日志；API Key 与平台令牌只保存在应用私有存储

## 1.1.0 · 2026-09-14

- 首个 Android 移植版本：悬浮窗、吸附物理、余额与用量、设置界面
